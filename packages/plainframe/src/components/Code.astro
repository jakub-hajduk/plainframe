---
import { Code as ECCode } from 'astro-expressive-code/components'

interface Props {
  code?: string;
  lang?: string;
  className?: string;
}

const { code, lang, className } = Astro.props
---

<ECCode lang={lang} code={code || ''} class={className || ''}/>

