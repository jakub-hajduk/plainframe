---
import { resolveMetaTitle } from './csf'

interface Props {
  title?: string
  of?: unknown
}

const { title, of } = Astro.props as Props
const resolvedTitle = resolveMetaTitle(of, title)
---

{resolvedTitle ? <div hidden data-storybook-title={resolvedTitle} /> : null}
