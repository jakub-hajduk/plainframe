<div class="full-width hero">
    <div class="inner">
      <slot />
    </div>
</div>

<style>
    .hero {
        background-color: var(--plainframe-color-primary-100);
        color: var(--plainframe-color-tinted-1200);
        height: calc( 6 * var(--plainframe-size-large-1300));
        margin-bottom: var(--plainframe-space-medium-700);
        border-bottom: 1px solid var(--plainframe-color-primary-400);
        padding: var(--plainframe-space-medium-1300) 0;
    }
</style>
